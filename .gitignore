__pycache__/
*.pyc
.ipynb_checkpoints/
log.txt
*.png
!docs/*.png
.DS_Store
.venv/

# assets/ is ignored by default, but the committed default image is tracked
# so Streamlit Community Cloud (which builds from the repo) can display it.
assets/*
!assets/.gitkeep
!assets/cat-meme.jpg
